# 异种起源：无尽洪流 - Godot 垂直切片

## 仓库内项目索引

本仓库包含两个相互隔离的 Godot 4 项目，需要分别导入、运行和构建：

| 项目路径 | 界面规格 | 运行 | 验证 |
| --- | --- | --- | --- |
| 仓库根目录 `./` | 540x960 竖屏技术切片 | `godot4 --path .` | `godot4 --headless --path . -s res://tests/core_rules.gd` |
| `variants/desktop_v1/` | 1600x900 桌面第一版（最小 1280x720） | `godot4 --path variants/desktop_v1` | `cd variants/desktop_v1 && ./scripts/verify.sh` |

两个项目保留各自的入口、Autoload 与存档契约；存档不互读，也不做自动迁移。以下内容仅描述仓库根目录项目。

## 验收与用户研究

桌面第一版的真人游玩、事件采集、PXI、缺陷分级和逐项通过标准见 [验收游玩测评包](doc/验收游玩测评/README.md)。该文档包与自动化测试互补，自动化规则通过不等同于真人可玩性验收通过。

## 当前桌面候选状态

`variants/desktop_v1/` 当前达到的交付级别是 **Linux 可玩预发布候选（单场验证通过）**，不是正式第一版完成或发布通过。已验证玩法基线为 `8f23b056b2ebbeac52d0946987f1716356552d01`：技术验证与架构审查通过，并由独立 agent 在 Godot 4.7、Linux/X11、干净用户目录和项目正式入口下使用 OS 鼠标键盘输入完成一次 613 秒场次，`GAME-ACCEPT-001` 为 8/8。

该证据只支持上述构建和环境中的单场可玩性，不支持 Windows、正式分层真人样本、PXI、最低硬件、45/60 分钟内容容量、完整 DPI/缩放、真实音频、物理设备或商业发布就绪声明。正式终验证据完整性仍为 `INVALID / NOT_RUN`。完整边界、已知问题和升级条件见 [Linux 可玩预发布候选交付声明](doc/验收游玩测评/07_Linux可玩预发布交付声明.md)。

## 运行

```bash
godot --path .
godot --headless --path . --quit-after 3
```

主场景：`res://scenes/app/Main.tscn`。项目以 540x960 竖屏入口运行。

## 核心规则验证

```bash
godot --headless --path . -s res://tests/core_rules.gd
```

该脚本验证资源不为负、器官升级成本、储备孵化与投放消耗、吞噬度推进、撤离保留兵力、插件改变战斗、离线 12 小时封顶、重置预览与实际收益一致、存档读写。

## 首局观察路径

1. 等待有机原浆自动增长，升级菌毯绒毛和深钻根须。
2. 在战场面板左侧点击蓄兵，观察波次槽位、缺口叉号、敌压阈值和 4% 重置预览目标线。
3. 储备接近阈值后点击中线强攻，观察补位进入、前线推进、敌压削减和螺旋奖励变化。
4. 升级菌毯/深钻根须或装配酸化甲壳后回到战场，观察波次阈值、有效敌压、损耗和构筑图标变化。
5. 在战场右侧点击撤离保全兵力，观察回流箭头和回收点阵。
6. 达到 4% 目标后查看战场目标线和吞噬世界预览，确认突变原收益已经出现。

## 当前边界

这是技术垂直切片和规则样机的游戏化返工版本，不宣称第一版游戏通过；它只把首局核心循环提升为可观察的战线、单位、敌压、补位和损耗反馈。项目仍不包含 Android 正式包、完整实时虫群战场、星际/深海/泰坦/PvP/服务器经济，也没有完整商业 MVP 内容量。数据结构已保留扩展到更多区域、单位、敌人、元素和插件的入口。
